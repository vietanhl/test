import React from 'react';
import styled from 'styled-components';

const Line = () => {
  const StyledLine = styled.hr`
    color: #282c34;
    width: 100%;
  `;
  return <StyledLine />;
};

export default Line;
